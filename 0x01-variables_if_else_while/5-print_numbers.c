#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: print all single numbers of base 10
 *
 * Return: Always 0 (success)
 */
int main(void)
{
	int a;

	for (a = 0; a<= 9; a++)
	{
		printf(a);
	}
	printf('\n');
	return (0);
}
